namespace SsmsResultsGrid.Core.Filtering
{
    /// <summary>Filter match semantics. Integer values line up with the mode ComboBox indexes.</summary>
    public enum FilterMode
    {
        Contains = 0,
        StartsWith = 1,
        Exact = 2
    }
}
